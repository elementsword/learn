#### 1.vector写一万个元素，要怎么提升他的性能

reverse提前分配空间  

使用emplace_back而不是push_back（直接构造）pushback会拷贝‘

push_back 也可以用移动拷贝 

#### 2.C++里面指针和引用的区别

指针

int p=20;

int * a =&p;

引用

int p=20；

int&a =p

#### 引用使用 

传参 避免拷贝

返回值引用 可以修改原变量  但是注意不要返回局部的 否则会悬空 因为原变量会被删除

#### 3.字节

bool 1字节

char 1个字节

int 4字节

float 4字节

double 8字节

地址一般8字节

#### 4.多态

静态多态 

函数重载 加模版 

动态多态 

主要目的 统一接口设计 虚函数 父类 子类继承 使用 

多态对析构函数有啥要求 应该就是每个子类都需要继承虚析构函数自己实现吧

#### 5  把子类对象赋值给基类对象，重写的方法，调用的是子类的方法还是基类的方法 

如果直接赋值 那使用就是基类的

通过指针指向 就是子类

#### 6.调用析构函数的顺序  和构造函数顺序

子类->父类 

父类->子类



#### 7.怎么vector预先申请指定大小的空间 

reverse resize

#### 8map和unordermap

map			   使用红黑树 	 ologn时间复杂度 查询  	顺序(按key排序 )

unorderedmap	hash表 		o1时间复杂度 查询 		顺序(无序)

不关心 key 顺序，只关注快速查找/插入/删除

unordered_map

需要 key 排序，或按 key 范围查找

map
